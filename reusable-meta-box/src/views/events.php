// Event date
<input type="date">

// Event location (City, State); make these separate fields so that they may be searched
<input type="text">
// Escape input with 'esc_attr()'

// Name of performance venue (e.g. First Presbyterian Church)
<input type="text">
// Escape input with 'esc_attr()'

// Street address of performance venue (address 1, address 2, city, state, zip
<input type="text">
// Escape input with 'esc_attr()'

// Event start time
<input type="time">
// Escape input with 'esc_attr()'

// Admission price
<!--either <input type="number"> or <input type="text">-->
// Escape input with 'esc_attr()'

// Phone (event sponsor phone number)
<input type="tel">
// Escape input with 'esc_attr()'

// Map to event venue (Google Maps url)
<input type="url">
// Escape input with 'esc_url()'

// Image of event venue (image url pulled from Media Library; what to replace it with if there is no image?)
<input type="url">
// Escape input with 'esc_url()'